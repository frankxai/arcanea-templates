# arcanea-templates — Claude Instructions

Read `AGENTS.md` and `.agent-harness.json` first. This is template infrastructure, not a production app.

Keep changes portable, avoid hidden global dependencies, and do not run heavyweight automation from stop hooks.

